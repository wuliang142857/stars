{
  "name": "stars",
  "version": "1.0.0",
  "description": "Account credentials management API backed by Cloudflare D1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
